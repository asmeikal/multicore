package homework_seconda_implementazione;


public class WorkerThread
{
	// stessi metodi, reimplementare allocando meno oggetti possibili

	public void run()
	{
		// this.length = this.sg.length
		// byte[] radice = this.asg.getNextWord();
		// byte[] parola = new byte[radice.length() + this.length];
		// Arrays.copy(parola, radice);
		// while (sg.hasWords()) {
		// sg.getNextWord(parola, radice.length());
		// byte[] hash = md5.hash(parola);
		// }
	}
}
